- if current_user
  p = link_to t('post.my'), my_posts_path
  p = link_to t('post.add'), new_post_path

table.posts
  - @posts.each do |post|
    tr
      td= link_to post.title, post
      td= post.body
      td= post.user.email

= will_paginate @posts
